// export const isAllowed = (user, rights) =>
//   rights.some(right => user.rights.includes(right));

  export const hasrole = localStorage.getItem('roles');
// export const hasRole = (user, role) =>
// role.some(rolee => user.roles.includes(rolee));